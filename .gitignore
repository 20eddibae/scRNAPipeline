.env
.env.*
!.env.example
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.ipynb_checkpoints/
# The dataset cache at the repo root only. Unanchored, this rule matches any
# directory called `data` at any depth, which silently swallowed the web app's
# `src/data/` modules.
/data/
runs/*
!runs/.gitkeep
*.h5ad
*.tar.gz
.modal/
.pytest_cache/

# the web demo is static; nothing here is built
web/node_modules/
web/.vercel/
experiments/results/
