"""Agentic scRNA-seq pipeline.

Claude drives the run (which step next, how to read the markers); Jev answers the
typed decision points inside each step (thresholds, method choice, resolution).
"""

__version__ = "0.1.0"
